include ../includes/mixins
doctype html
block vars
html(lang="ru-RU")
    head
        meta(charset="UTF-8")
        //- meta(name="viewport" , content="width=device-width, initial-scale=1")
        meta(name="viewport" content="width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1")
        meta(name="description" content='MOST technology is an EdTech startup that provides a new experience in talent development for education institutions, employers and specialists.')
        title #{title}
        include ../includes/parts/links
    body
        block header

        block content

        block footer
